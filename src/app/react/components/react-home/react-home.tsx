import { Text } from '../../contexts/language-context/language-context';

type AppProps = {};

export const ReactHome = ({}: AppProps) => {

  return (
    <div>
      <Text tid='ttest'/>
    </div>
  );
};
