import * as React from 'react';
import { FunctionComponent, useState } from 'react';

type AppProps = {};

export const ReactHome = ({}: AppProps) => {

  return (
    <div>
      Hola Mundo
    </div>
  );
};
